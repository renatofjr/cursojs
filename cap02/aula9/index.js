// String, number, undefined, null, boolean, symbol

const nome = 'Renato'; // String
const nome1 = 'Renato'; // String
const nome2 = 'Renato'; // String
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined = não aponta para nenhum local na memória
const sobrenomeAluno = null // nulo -> também não aponta para nenhum local na memória
const boolean1 = true;
const boolean2 = false;
