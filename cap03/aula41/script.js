// Escreva uma função que recebe 2 números e retorne o maior deles

function numMax(x, y) {
    return x > y ? x : y;
}

const numMax2 = (x, y) => x > y ? x : y;

console.log(numMax(2,4));
console.log(numMax2(10,15));